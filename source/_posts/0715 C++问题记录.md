## 0715 C++问题记录

### 左右指针

* 1、二分查找。（有序容器）
* 2、n数之和。对于有序容器，左右相加判断大小区间，类似二分法。
* 3、反转数组。左右两个指针相互交换往中间靠。
* 4、回文串判断。一左一右两个指针相向而行。
* 5、最长回文串。找回文串的难点在于，回文串的的长度可能是奇数也可能是偶数，解决该问题的核心是**从中心向两端扩散的双指针技巧**。工具函数找回文串，总函数判断是不是最长的。
  * 写代码出现的小问题：string的长度用 `length()`,`s.substr(l,n)`用于返回s从第l个开始的n长度的字符串。